# Module: components/SectionLayout

**Path:** `dianych-website/app/components/SectionLayout.tsx`

## Purpose

Section wrapper with translated `<h2>` from `titleKey`.

## Public interface

- `{ id, titleKey, children }` — unknown keys render as the raw key.

## Dependencies

`lib/translations`.

## Consumers

`Frames` only (gallery sections use `Gallery` instead).

## Tests

None.
