# Module: components/LanguageSwitcher

**Path:** `dianych-website/app/components/LanguageSwitcher.tsx`

## Purpose

Dropdown to set `ua` / `en` via `useLanguage`. Click-outside closes.

## Public interface

Default export component. No props.

## Dependencies

`contexts/LanguageContext`, `next/image` (flag SVGs under `/images/flags/`).

## Consumers

`Header`, `TopBar`.

## Tests

None.
