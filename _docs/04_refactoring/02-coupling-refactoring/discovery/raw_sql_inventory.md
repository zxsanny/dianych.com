# S22 raw SQL inventory

**Scan method**: ripgrep `SELECT|INSERT|UPDATE|DELETE|FROM |execute\(|sql\`` on `dianych-website/**/*.{ts,tsx,js}` excluding `node_modules` / `.next`.

**Hits**: none. Product has no database.

| file | line | API | sql_preview | change_id / deferral |
|------|------|-----|-------------|----------------------|
| — | — | — | — | n/a |
